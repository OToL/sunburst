#pragma once

#include "std.h"

#include SB_SRC_STD_INC(cstring)

namespace SB_WSTD_NS {

      SB_SRC_STD_USE(strrchr);
      SB_SRC_STD_USE(strlen);
      SB_SRC_STD_USE(strncpy);
      SB_SRC_STD_USE(strncmp);
      SB_SRC_STD_USE(strcpy);
      SB_SRC_STD_USE(strncat);
      SB_SRC_STD_USE(strcat);
      SB_SRC_STD_USE(memset);
      SB_SRC_STD_USE(memcpy);

}
