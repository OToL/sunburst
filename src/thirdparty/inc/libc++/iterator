#pragma once

#include "prolog.h"

#include SB_SRC_STD_INC(iterator)

SB_STD_NS_BEGIN

	SB_STD_USE(begin);
	SB_STD_USE(end);
    SB_STD_USE(extent);
	SB_STD_USE(iterator_traits);
	SB_STD_USE(random_access_iterator_tag);
    SB_STD_USE(back_inserter);
    SB_STD_USE(size);

    namespace ext {
        template <typename T>
        using iterator_tag_t = typename wstd::iterator_traits<T>::iterator_category;
    }

SB_STD_NS_END
