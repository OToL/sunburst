#pragma once

#include "base.h"

#include SB_SRC_STD_INC(iterator)

namespace SB_WSTD_NS {

    SB_SRC_STD_USE(begin);
	SB_SRC_STD_USE(end);
    SB_SRC_STD_USE(extent);
	SB_SRC_STD_USE(iterator_traits);
	SB_SRC_STD_USE(random_access_iterator_tag);
    SB_SRC_STD_USE(back_inserter);
    SB_SRC_STD_USE(size);

namespace SB_WSTD_EXT_NS {

    template <typename T>
    using iterator_tag_t = typename wstd::iterator_traits<T>::iterator_category;

}}
