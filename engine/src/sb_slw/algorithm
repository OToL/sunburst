#pragma once

#include "_impl/std_wrap.h"
#include SB_SRC_STD_INC(algorithm)

namespace SB_STD_NS {

    SB_SRC_STD_USE(equal);
    SB_SRC_STD_USE(all_of);
    SB_SRC_STD_USE(find);
    SB_SRC_STD_USE(find_if);
    SB_SRC_STD_USE(distance);
    SB_SRC_STD_USE(copy);
    SB_SRC_STD_USE(fill);
    SB_SRC_STD_USE(distance);
    SB_SRC_STD_USE(swap);
    SB_SRC_STD_USE(swap_ranges);
    SB_SRC_STD_USE(min);
    SB_SRC_STD_USE(max);
    SB_SRC_STD_USE(move);
    SB_SRC_STD_USE(replace);
    SB_SRC_STD_USE(sort);
    SB_SRC_STD_USE(uninitialized_fill);

}
