#pragma once

#include "prolog.h"
#include SB_SRC_STD_INC(algorithm)

namespace SB_STD_NS {

    SB_STD_USE(find);
    SB_STD_USE(find_if);
    SB_STD_USE(distance);
    SB_STD_USE(copy);
    SB_STD_USE(fill);
    SB_STD_USE(distance);
    SB_STD_USE(swap);
    SB_STD_USE(swap_ranges);
    SB_STD_USE(min);
    SB_STD_USE(max);
    SB_STD_USE(move);

}
